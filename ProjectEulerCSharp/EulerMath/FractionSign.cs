namespace ProjectEulerCSharp.EulerMath
{
    /// <summary>
    /// Follows the same semantics as the integer based Sign method of INumber, but without a Zero value.
    /// </summary>
    public enum FractionSign
    {
        Positive = 1,
        Negative = -1,
    }
}